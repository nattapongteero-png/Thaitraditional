
  # UI Prototype Creation

  This is a code bundle for UI Prototype Creation. The original project is available at https://www.figma.com/design/QOsbIu39yEi6rxGXDIohzy/UI-Prototype-Creation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  